{
  "name": "colorku-src",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Source + build for the /colorku page. Bundles SudokuGenerator.jsx (colorku puzzle generator with shared-board persistence) into a single self-contained ../colorku/index.html.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
