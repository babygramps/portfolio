{
  "name": "toilet-src",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Source + build for the Fieldhouse Restroom Co. booking prototype at /toilet. Bundles the React app and its stylesheet into a single self-contained ../toilet/index.html.",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "vitest": "^2.1.8"
  }
}
